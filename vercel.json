{
  "buildCommand": "cd fund-dashboard && npm install && npm run build",
  "outputDirectory": "fund-dashboard/dist",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/$1" },
    { "source": "/((?!api/).*)", "destination": "/index.html" }
  ]
}
